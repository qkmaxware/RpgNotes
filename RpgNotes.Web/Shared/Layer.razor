<div style="position: absolute; left: 0; top: 0; user-select: none">
    @ChildContent
</div>

@code {
    [CascadingParameter] private Map Map { get; set; }
    [Parameter] public RenderFragment ChildContent {get; set;}

    protected override void OnInitialized() {
        Map?.AddLayer(this);
    }
}